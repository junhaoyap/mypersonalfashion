Router.route('/', {
  name: 'home'
});

Router.route('recommendations', {
  name:'recommendations'
});

Router.route('/selectbrands', {
  name: 'selectBrands'
});

Router.route('/feed', {
  name: 'feed'
});
